{
  "schema_version": 2,
  "description": "Lifecycle-visible dispatcher for bounded, low-risk work through native host delegation.",
  "profiles": {
    "claude": {
      "host_name": "Claude",
      "parent_channel": "`SendMessage` (use `ListAgents` to identify the parent)",
      "scope_contract": [
        "Accept bounded low-risk work that needs little interpretation: mechanical edits, straightforward audits and extraction, repetitive processing, and large-text triage or compression. Preserve unique relevant information when compressing. This tier deliberately overlaps the balanced tier for routine tasks; use it when the assignment does not need moderate reasoning. Return difficult debugging, material ambiguity, architecture, integration, security-sensitive judgment, and final validation to the parent."
      ],
      "conflict_contract": [
        "Use the same parent escalation path for an operation outside the assigned ownership; a single-use permission never becomes a standing grant."
      ],
      "lifecycle_contract": [
        "Return the evidence report through the foreground Agent result or `SendMessage` for a named background teammate. Claude automatically releases a foreground Agent when its result returns; do not issue a stop operation for that completed worker."
      ],
      "output": {
        "path": "claude/agents/bulk-worker.md",
        "format": "claude-markdown",
        "name": "bulk-worker",
        "model": "haiku",
        "effort": "medium",
        "max_turns": 30
      }
    },
    "codex": {
      "host_name": "Codex",
      "parent_channel": "the collaboration channel available to the worker",
      "scope_contract": [
        "Accept bounded low-risk work that needs little interpretation: mechanical edits, straightforward audits and extraction, repetitive processing, and large-text triage or compression. Preserve unique relevant information when compressing. This tier deliberately overlaps the balanced tier for routine tasks; use it when the assignment does not need moderate reasoning. Return difficult debugging, material ambiguity, architecture, integration, security-sensitive judgment, and final validation to the parent."
      ],
      "conflict_contract": [
        "If no parent channel is available, stop and report instead of guessing."
      ],
      "lifecycle_contract": [
        "Return the evidence report and stop. The host session lifecycle releases the worker; no nonexistent post-result worker action is required."
      ],
      "output": {
        "path": "codex/agents/bulk_worker.toml",
        "format": "codex-toml",
        "name": "bulk_worker",
        "model": "gpt-5.6-luna",
        "reasoning_effort": "medium"
      }
    }
  }
}
